{
  "name": "bidflow",
  "version": "1.0.0",
  "private": true,
  "description": "Production-ready MERN auction platform with realtime bidding.",
  "scripts": {
    "dev": "concurrently \"npm run dev -w bidflow-backend\" \"npm run dev -w bidflow-frontend\"",
    "install:all": "npm install -ws",
    "build": "npm run build -w bidflow-frontend",
    "start": "npm run start -w bidflow-backend"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
